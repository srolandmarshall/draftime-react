<%= form_with(model: fantasy_league, local: true) do |form| %>
  <% if fantasy_league.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fantasy_league.errors.count, "error") %> prohibited this fantasy_league from being saved:</h2>

      <ul>
        <% fantasy_league.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :max_teams %>
    <%= form.number_field :max_teams %>
  </div>

  <div class="field">
    <%= form.label :league_id %>
    <%= form.text_field :league_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
