<%= form_with(model: drafter, local: true) do |form| %>
  <% if drafter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(drafter.errors.count, "error") %> prohibited this drafter from being saved:</h2>

      <ul>
        <% drafter.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :owner %>
    <%= form.text_field :owner %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
